# CLAUDE.md — Project rules

## Git workflow

- **Không tự động commit và push.**
- Sau khi sửa code xong, chỉ build để kiểm tra lỗi (`npm run build --workspace client`).
- Để người dùng tự chạy `git add`, `git commit`, `git push` khi sẵn sàng.
